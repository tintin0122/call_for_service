pipeline {
    agent { label 'docker-slave' }
    tools {
        maven 'maven'
    }

    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}